<%= users.users %>



<% for (let user of users) { %>
<p><%- user.id %></p>
<p><%- user.user_name %></p>
  <% if (user.tasks) { %>
    <% for(let task of user.tasks) { %>
      <p><%- task.task_name %></p>
    <% } %>
  <% } %>
<% } %>